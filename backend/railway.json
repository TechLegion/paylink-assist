{
  "$schema": "https://railway.app/static/railway-service-schema.json",
  "deploy": {
    "startCommand": "python manage.py migrate && gunicorn config.wsgi --bind 0.0.0.0:$PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}
